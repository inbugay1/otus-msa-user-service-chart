apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-docker-service-chart.fullname" . }}
  labels:
    {{- include "test-docker-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-docker-service-chart.selectorLabels" . | nindent 4 }}
